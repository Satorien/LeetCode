## Step 1. Initial Solution

- そんなに難しくなさそうだが意外とすんなり書けない
    - ノードの位置がズレないように処理待ちのリストをつくっておきたい
    - 返すべきrootを作っておいてそこから更新していくのが良さそう
- 初めは直接ノードを変数に代入してしまっていた
    
    ```python
    new_node = TreeNode(val = node1.val+node2.val)
    ```
    
    - 連結リストをやっていた頃は理解できた気になっていたがまた忘れてしまった
        - 忘れなくなるまで定期的に復習しておきたい
- 結局dummy_rootを用いて一つのループでまとめて処理できるようにした

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def mergeTrees(self, root1: Optional[TreeNode], root2: Optional[TreeNode]) -> Optional[TreeNode]:
        dummy_root: TreeNode = TreeNode()
        new_nodes: List[TreeNode] = [dummy_root]
        nodes1: deque[TreeNode] = deque([root1, None])
        nodes2: deque[TreeNode] = deque([root2, None])

        def add_nodes(node1: Optional[TreeNode], node2: Optional[TreeNode]) -> Optional[TreeNode]:
                if node1 is None and node2 is None:
                    return None
                if node1 is None:
                    return node2
                if node2 is None:
                    return node1
                next_nodes1.extend([node1.left, node1.right])
                next_nodes2.extend([node2.left, node2.right])
                new_node = TreeNode(val=node1.val+node2.val)
                next_nodes.extend([new_node])
                return new_node

        while new_nodes:
            next_nodes = []
            next_nodes1 = deque()
            next_nodes2 = deque()
            for new_node in new_nodes:
                node1, node2 = nodes1.popleft(), nodes2.popleft()
                new_node.left = add_nodes(node1, node2)
                node1, node2 = nodes1.popleft(), nodes2.popleft()
                new_node.right = add_nodes(node1, node2)
            new_nodes = next_nodes
            nodes1 = next_nodes1
            nodes2 = next_nodes2
        return dummy_root.left
```

### Complexity Analysis

- 時間計算量：O(min(m,n))
    - tree1とtree2のノードが少ない方のノード数までのチェックで済む

## Step 2. Alternatives

- 少し無駄な部分があったので改めて整形すると以下のように書ける
    - pop2回の代わりに[left,right]をdequeに入れている

```python
class Solution:
    def mergeTrees(self, root1: Optional[TreeNode], root2: Optional[TreeNode]) -> Optional[TreeNode]:
        dummy_root: TreeNode = TreeNode()
        new_nodes: deque[TreeNode] = deque([dummy_root])
        nodes1: deque[TreeNode] = deque([[root1, None]])
        nodes2: deque[TreeNode] = deque([[root2, None]])

        def add_nodes(node1: Optional[TreeNode], node2: Optional[TreeNode]) -> Optional[TreeNode]:
                if node1 is None and node2 is None:
                    return None
                if node1 is None:
                    return node2
                if node2 is None:
                    return node1
                nodes1.append([node1.left, node1.right])
                nodes2.append([node2.left, node2.right])
                new_node = TreeNode(val=node1.val+node2.val)
                new_nodes.append([new_node])
                return new_node

        while new_nodes:
            new_node = new_nodes.popleft()
            node1_left, node1_right = nodes1.popleft()
            node2_left, node2_right = nodes2.popleft()
            new_node.left = add_nodes(node1_left, node2_left)
            new_node.right = add_nodes(node1_right, node2_right)
        return dummy_root.left
```

- やはり今回も再帰でうまく書けると簡潔に書ける
    - `if root1 is None and root2 is None: return None` はなくても動くが好みの範囲か
        - https://github.com/shintaro1993/arai60/pull/27/files#diff-b1d08e4d77e2f992323c919a01d63a59f8d9bf7271b218431844d4e356bb7beaR258

```python
class Solution:
    def mergeTrees(self, root1: Optional[TreeNode], root2: Optional[TreeNode]) -> Optional[TreeNode]:
        if root1 is None and root2 is None:
            return None
        if root1 is None:
            return root2
        if root2 is None:
            return root1
        root = TreeNode(val=root1.val+root2.val)
        root.left = self.mergeTrees(root1.left, root2.left)
        root.right = self.mergeTrees(root1.right, root2.right)
        return root
```

- こういう処理の仕方をするとコードを読む際の負荷は下がるという理解ができた
    - https://discord.com/channels/1084280443945353267/1262688866326941718/1297934906189549599

```jsx
func left(node *TreeNode) *TreeNode {
    if node == nil {
        return nil
    }
    return node.Left
}
```

- そのままでも別に書き換えてはいないから良いかとおもっていたが、確かにdeepcopyしないと関数の返り値をいじったらroot1やroot2まで変わってしまう可能性に思い至った
    - https://github.com/hayashi-ay/leetcode/pull/12/files#diff-9bdf6ea146707cd300caa32b58d1af7701eda07a7d2e9802408cd8ce1cc70720R76

```python
        if root1 is None:
            return copy.deepcopy(root2)
        if root2 is None:
            return copy.deepcopy(root1)
```

## Step 3. Final Solution

- シンプルさから再帰を選択

```python
class Solution:
    def mergeTrees(self, root1: Optional[TreeNode], root2: Optional[TreeNode]) -> Optional[TreeNode]:
        if root1 is None:
            return copy.deepcopy(root2)
        if root2 is None:
            return copy.deepcopy(root1)
        root = TreeNode(val=root1.val+root2.val)
        root.left = self.mergeTrees(root1.left, root2.left)
        root.right = self.mergeTrees(root1.right, root2.right)
        return root
```
